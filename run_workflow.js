// This file replaces the Express server workflow with the Go server
console.log("Node.js/Express server has been removed from this project.");
console.log("The Go server is now the only backend for this application.");
console.log("To start the application, run './run-minimal-go.sh'");

// Simply exit with success status to prevent the workflow from failing
process.exit(0);